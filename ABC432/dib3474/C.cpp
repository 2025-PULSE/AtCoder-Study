#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;

int main() {
    int n, x, y;
    cin >> x;

    vector<int> child(n);
    for (int &e : child) cin >> e;

    vector<vector<pair<int, int>>> distribute(n);

    // gg
}